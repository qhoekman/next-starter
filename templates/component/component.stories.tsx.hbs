import { Meta, StoryFn } from '@storybook/react';

import { {{pascalCase name}} } from './{{kebabCase name}}';

export default {
  title: '{{prefix}}/{{titleCase name}}',
  component: {{pascalCase name}},
} as Meta<typeof {{pascalCase name}}>;

const Template: StoryFn<typeof {{pascalCase name}}> = (args) => <{{pascalCase name}} {...args}></{{pascalCase name}}>;

export const Default = {
  render: Template,
  args: {},
};

